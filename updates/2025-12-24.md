# 2025-12-24

## 作業内容
- Secret Managerから`perplexity_api_key`を読み込むよう`load-env.sh`を更新
- python-dotenv導入でローカル環境の環境変数読み込みに対応
- LangChainのPydanticOutputParser導入でプロファイル生成を型安全に
- GitHub API拡張（ディレクトリ構造、依存ファイル、コードサンプル取得）
- pytest作成（22テストケース）

## 学び
- VS Codeの`envFile`設定はStreamlitのサブプロセスには効かない → python-dotenvでアプリ側で読み込む必要がある
- LangChainのPydanticOutputParserを使うとLLMレスポンスのJSON解析エラーを型安全に処理できる
- GitHub APIの`repo.get_contents()`でリポジトリ内のファイル内容を取得できる
