# 2025-12-28

## 作業内容
- GitHub OAuth認証機能の実装 (`services/auth.py` 新規追加)
- 求人検索の希望条件機能強化 (`JobPreferences` dataclass追加 - 勤務地・年収・働き方・職種等)
- Cloud Build CI/CD設定追加 (`cloudbuild.yaml`, `terraform/cloudbuild.tf`)
- Terraform拡張 (GitHub OAuth用Secret Manager参照、Cloud Run環境変数追加)
- テストコード修正 (`JobPreferences`対応)
- pytest更新 (9.0.2へアップグレード)
- Cloud Run再デプロイ (最新シークレット反映)

## 学び
- Secret Managerの `version = "latest"` を指定していても、Cloud Runは起動時にシークレットを読み込むため、シークレット更新後は再デプロイが必要
- GitHub OAuth Appはコールバック URLが異なるため、ローカル開発用とCloud Run本番用で別々に作成が必要
