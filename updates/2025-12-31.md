# 2025-12-31

## 作業内容
- Firestoreプロファイルキャッシュ機能の実装（`services/cache.py`新規作成）
- リポジトリ情報のキャッシュ機能追加
- ユーザー設定（希望条件）のFirestore永続化
- `st.session_state`による分析結果の保持（rerun時の再分析防止）
- Streamlit `on_click`コールバックパターンの採用
- Claude Code hooks設定（ruff check自動実行）
- Terraform `prevent_destroy`によるFirestoreリソース保護

## 学び
- Terraformの`lifecycle { prevent_destroy = true }`で`terraform destroy`時にリソースを保護できる
  - Firestoreのようなデータを持つリソースに設定すると、誤削除を防げる
  - 他のリソースは通常通り削除される
